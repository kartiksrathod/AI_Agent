================================================================
               🔐 PERMANENT ADMIN CREDENTIALS
================================================================

IMPORTANT: These credentials are PERMANENT and should NOT be changed

📧 Email:    kartiksrathod07@gmail.com
🔑 Password: Sheshi@1234
👤 Name:     Kartik S Rathod
👑 Role:     Administrator

================================================================
               CREDENTIAL PROTECTION NOTICE
================================================================

⚠️  DO NOT MODIFY THESE CREDENTIALS
⚠️  These are set as permanent admin access credentials
⚠️  Stored in: /app/backend/.env (ADMIN_EMAIL, ADMIN_PASSWORD)
⚠️  Database is configured to persist at: /data/db

================================================================
               RECOVERY INSTRUCTIONS
================================================================

If admin password is accidentally changed, restore it by running:

cd /app/backend
python3 << 'EOF'
from pymongo import MongoClient
from passlib.context import CryptContext
import os

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
client = MongoClient('mongodb://localhost:27017')
db = client['academic_resources']

# Restore permanent password
permanent_password = "Sheshi@1234"
hashed_pw = pwd_context.hash(permanent_password)

db.users.update_one(
    {"email": "kartiksrathod07@gmail.com"},
    {"$set": {"password": hashed_pw}}
)
print("✅ Admin password restored to: Sheshi@1234")
EOF

================================================================
               DATA PERSISTENCE
================================================================

✅ MongoDB data directory: /data/db
✅ Database name: academic_resources
✅ Data persists permanently (survives restarts)
✅ Automatic backups stored in: /app/backups/

================================================================
Created: October 21, 2025
Last Updated: October 21, 2025
================================================================
